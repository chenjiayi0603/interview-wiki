# 关系数据库三大范式

**参考**：https://www.jianshu.com/p/a164509264ff

## 第一范式（1NF）

1NF是指数据库中表的每一列都是不可分割的基本数据项，同一列的数据不能有多个值。如果出现重复，就可能需要定义一个新的实体来描述关系。

> 在任何关系型数据库中，1NF是关系模式的基本要求，没有满足1NF的数据库就不是关系数据库。（每一列都是不可分割的基本数据项）

## 第二范式（2NF）

2NF是在1NF基础上建立的。2NF要求数据库中表的每个实例或行必须能唯一的区分。为实现区分通常需要为表加上主键。

2NF要求实体的属性完全依赖于主键。所谓完全依赖是指不能存在仅依赖主键一部分的属性。

> 简而言之，第二范式就是非主属性不能部分依赖于主关键字。（不能存在仅依赖主键一部分的属性）

## 第三范式（3NF）

3NF依赖于2NF。3NF要求一个数据库表中不包含在其他表中已经存在的非主键的字段。

> 3NF是表属性不能依赖其他表中非主键的属性。（不能依赖其他表中非主键）

[src: raw/ingested/2技术/mysql/数据库基础概念与范式设计.md]

## Related Pages
- [[ER图]]
- [[MySQL索引]]
- [[SQL基础查询]]
