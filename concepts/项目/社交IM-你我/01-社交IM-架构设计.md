# 社交 IM 架构设计

> 系统架构、技术栈演进、分库分表方案、性能数据。

---

## 一、系统架构

### 1.1 最终架构

```
┌────────────────────────────────────────────────────┐
│                    客户端层                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │  iOS     │  │  Android │  │  H5      │          │
│  └──────────┘  └──────────┘  └──────────┘          │
└──────────────────────┬──────────────────────────────┘
                  TCP长连接 + HTTP短连接
                       ▼
┌────────────────────────────────────────────────────┐
│                  接入层 (Nginx)                     │
│  负载均衡 + SSL 终结 + 限流                         │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌────────────────────────────────────────────────────┐
│                    服务层                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐│
│  │ IM 服务   │ │ 社交服务  │ │ 金融服务  │ │ 推送服务││
│  │ 单聊/群聊 │ │ 好友/动态 │ │ 借贷/理财 │ │APNs/个推││
│  │ 消息存储  │ │ 点赞/评论 │ │ 支付/风控 │ │ 短信通知││
│  └──────────┘ └──────────┘ └──────────┘ └────────┘│
└──────────────────────┬──────────────────────────────┘
                       ▼
┌────────────────────────────────────────────────────┐
│                   存储层                             │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐      │
│  │ MySQL      │ │ Redis      │ │ MongoDB    │      │
│  │ 32库×64表   │ │ Codis 16节点│ │ 历史消息    │      │
│  │ 分库分表    │ │ 128GB      │ │ 冷数据存储  │      │
│  └────────────┘ └────────────┘ └────────────┘      │
└────────────────────────────────────────────────────┘
```

### 1.2 架构演进

| 阶段 | 时期 | 架构 | 问题 | QPS |
|------|------|------|------|-----|
| V1.0 | 2015 | 单体应用 + 单机 MySQL | 单点故障，扩展性差 | 2,000 |
| V2.0 | 2016 | 垂直拆分 + Redis 缓存 | 消息表数据量太大 | 5,000 |
| V3.0 | 2017 | 水平分片 + Codis 集群 | 分片后查询需要聚合 | 15,000 |
| V4.0 | 2018 | 服务化 + 消息队列削峰 | 运维复杂度增加 | 15,000（稳定） |

---

## 二、分库分表方案

### 2.1 分片规则

```cpp
// 用户 ID 分片：32 个库
int GetShardId(uint64_t user_id) {
    return user_id % 32;
}

// 每库 64 张表
int GetTableId(uint64_t user_id) {
    return (user_id / 32) % 64;
}

// 完整表名
string GetTableName(uint64_t user_id) {
    int shard_id = GetShardId(user_id);
    int table_id = GetTableId(user_id);
    return "message_" + to_string(shard_id) + "_" + to_string(table_id);
}
```

### 2.2 路由中间件

```cpp
class DBRouter {
public:
    mysql_conn* GetConnection(uint64_t user_id) {
        int shard_id = user_id % 32;
        return connection_pool_[shard_id].GetConnection();
    }

    ResultSet Query(uint64_t user_id, const string& sql) {
        auto conn = GetConnection(user_id);
        string table_name = GetTableName(user_id);
        string real_sql = ReplaceTable(sql, table_name);
        return conn->Query(real_sql);
    }

private:
    vector<ConnectionPool> connection_pool_;  // 32 个连接池
};
```

### 2.3 容量验证

```
用户数：1000 万
每用户平均消息：100 条
总消息量：10 亿条

分片后：
- 32 库 × 64 表 = 2048 张表
- 每表约 10亿 / 2048 = 48.8 万条
- 满足单表 < 500 万的设计目标
```

### 2.4 数据库性能

| 指标 | 数值 |
|------|------|
| 分库数量 | 32 |
| 每库分表 | 64 张 |
| 单表数据量 | < 500 万 |
| 查询延迟 | < 10ms（单表） |
| 跨库查询 | 50-200ms（需聚合） |
| 主从延迟 | < 1s（异步复制） |

---

## 三、Redis 集群

| 指标 | 数值 |
|------|------|
| Codis 节点数 | 16 |
| 单节点内存 | 8GB |
| 总内存 | 128GB |
| QPS | 100,000+ |
| 命中率 | 95% |

---

## 四、性能数据

### 4.1 QPS 数据

| 场景 | 峰值 QPS | 平均 QPS | 说明 |
|------|---------|---------|------|
| 消息发送 | 5,000 | 2,000 | 早高峰 8-9 点 |
| 消息推送 | 8,000 | 3,000 | 广播消息 |
| 在线状态查询 | 20,000 | 8,000 | 好友列表刷新 |
| 好友关系操作 | 3,000 | 1,000 | 添加/删除 |
| 动态发布 | 500 | 200 | 朋友圈 |
| **整体峰值** | **15,000** | **5,000** | 混合场景 |

### 4.2 峰值时间分布

```
06:00-08:00  1,000 QPS   起床高峰
08:00-09:00  15,000 QPS  早高峰（最大）
12:00-14:00  8,000 QPS   午休
18:00-20:00  10,000 QPS  晚高峰
22:00-24:00  5,000 QPS   睡前
```

### 4.3 框架性能实测

| 场景 | QPS | 说明 |
|------|-----|------|
| HTTP 裸响应 | 23,256 | 单节点单进程，6核 16GB |
| 写路径（Web → DataProxy → Redis → DBAgent → MySQL） | 1,861 | MySQL 单分片为瓶颈 |
| 读路径（Web → DataProxy → Redis → DBAgent → MySQL） | 929 | 同写路径 |
| DataProxy → Redis/SSDB 读 | 14,658 | string 类型读 |
| DataProxy → Redis/SSDB 写 | 12,228 | string 类型写 |

### 4.4 性能优化历程

| 阶段 | 问题 | 优化 | 效果 |
|------|------|------|------|
| V1.0 | 单机 MySQL 扛不住 | 读写分离 | QPS 2,000 → 5,000 |
| V2.0 | 写入瓶颈 | 分库分表 | QPS 5,000 → 15,000 |
| V3.0 | 缓存穿透 | 布隆过滤器 | 穿透率降 90% |
| V4.0 | 峰值抖动 | 消息队列削峰 | 抖动降 80% |
